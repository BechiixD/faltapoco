---
import Counter from "../Layouts/Counter.astro";
import { pages } from "../data/pages.js";
const { date, meta, content } = pages.cyberMonday;
---

<Counter date={date} meta={meta} content={content} />
