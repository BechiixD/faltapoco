---
import Layout from "../Layouts/Layout.astro";
import { calcTimeLeft } from "../utils/calcTimeLeft";
const targetIso = "2025-11-28T00:00:00Z";
const initial = calcTimeLeft(targetIso);

// Fecha en formato legible
const bfDate = new Date(targetIso);
const day = bfDate.getUTCDate();
const month = bfDate.toLocaleString("es-ES", { month: "long" });
const year = bfDate.getUTCFullYear();
const daysLeft = initial.days;

const pageTitle = "¿Cuánto falta para el Black Friday Argentina 2025? | Faltapoco.com";
const description =
    "Descubre exactamente cuántos días, horas y minutos faltan para el Black Friday Argentina 2025. Contador en tiempo real, fecha oficial y curiosidades sobre el evento de descuentos más grande.";

const url = "https://faltapoco.com/black-friday";
const image = "https://faltapoco.com/og/black-friday-argentina.png";
---

<Layout title="¿Cuánto falta para Black Friday 2025? | Faltapoco.com">
    <Fragment slot="head">
    <!-- Meta SEO -->
    <meta name="description" content={description} />
    <meta name="keywords" content="black friday argentina 2025, descuentos, ofertas, cuanto falta para black friday, faltapoco, contador black friday" />
    <link rel="canonical" href={url} />

    <!-- Open Graph -->
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={url} />
    <meta property="og:image" content={image} />
    <meta property="og:locale" content="es_ES" />

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={pageTitle} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={image} />

    <!-- Schema.org (JSON-LD) -->
    <script type="application/ld+json">
      {JSON.stringify({
        "@context": "https://schema.org",
        "@type": "WebPage",
        name: pageTitle,
        description,
        url,
        image,
        inLanguage: "es",
        publisher: {
          "@type": "Organization",
          name: "Faltapoco",
          url: "https://faltapoco.com"
        }
      })}
    </script>
  </Fragment>
    <main
        class="flex flex-col items-center justify-center flex-grow px-4 py-8 space-y-12 text-center md:gap-16"
    >
        <div class="flex flex-col items-center w-full space-y-4">
            <h1 class="text-4xl sm:text-5xl md:text-7xl font-extrabold mb-2">
                ¿Cuánto tiempo falta para el
                <span
                    class="bg-gradient-to-r from-[#0B5FFF] via-gray-400 to-[#0B5FFF] bg-clip-text text-transparent animate-gradient"
                    style="display: inline-block;"
                >
                    Black Friday Argentina 2025
                </span>?
            </h1>
        </div>
        <style>
            @keyframes gradient-move {
                0% {
                    background-position: 0% 50%;
                }
                50% {
                    background-position: 100% 50%;
                }
                100% {
                    background-position: 0% 50%;
                }
            }
            .animate-gradient {
                background-size: 200% 200%;
                animation: gradient-move 4s ease-in-out infinite;
            }
        </style>
        <div class="max-w-5xl w-full space-y-6 text-center mx-auto">
            <div
                class="p-4 sm:p-8 rounded-lg shadow text-center mx-auto bg-white"
            >
                <div
                    class="text-lg sm:text-xl md:text-2xl text-[#FFDD00] font-semibold"
                >
                    El Black Friday Argentina empieza el día {day} de {month} de {year}.
                </div>
                <div
                    class="mt-4 sm:mt-4 text-4xl sm:text-6xl md:text-8xl text-[#0B5FFF] font-mono"
                    data-countdown
                    data-target={targetIso}
                    aria-live="polite"
                >
                    {initial.days}d {String(initial.hours).padStart(2, "0")}h {
                        String(initial.minutes).padStart(2, "0")
                    }m {String(initial.seconds).padStart(2, "0")}s
                </div>
                <div class="mt-4 text-base sm:text-lg text-gray-600">
                    Faltan {daysLeft} días para el Black Friday Argentina 2025.
                </div>
            </div>
        </div>

        <script type="module" src="/js/countdown.js"></script>
        <script type="module">
            import { initCountdown } from "/js/countdown.js";
            // inicializa todos los [data-countdown]
            initCountdown();
        </script>
            <section class="text-center max-w-2xl mx-auto mt-6 text-gray-700 leading-relaxed">
                <p>
                    Pronto podrás integrar este contador en tu propia página o proyecto.
                    Estamos desarrollando una <strong>API gratuita</strong> que te permitirá obtener
                    el tiempo restante hasta cualquier evento importante, de forma sencilla y en tiempo real.
                </p>
                <p class="mt-3">
                    En Faltapoco queremos ofrecer <em>temporizadores precisos y personalizables</em> para fechas relevantes como
                    el Mundial, Navidad o Año Nuevo. Si querés sugerir un nuevo evento o mejorar el contador,
                    próximamente podrás hacerlo desde nuestra sección de comunidad.
                </p>
            </section>
    </main>
</Layout>
