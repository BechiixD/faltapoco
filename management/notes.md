First run ```pnpm run managment``` so it opens the local website and allows to modify the content easily.
Then run ```pnpm run create-astro``` so it creates all the new astro pages
And finally update the public/pages.json with ```pnpm run generate:json```